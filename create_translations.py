import os

with open("app/src/main/java/com/example/Translations.kt", "r") as f:
    original = f.read()

# I will write the python script to generate translations. 
# But wait, it's easier to just write the new file directly.
